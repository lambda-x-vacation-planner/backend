const express = require('express');
const router = express.Router();
const model = require('../model/post');

// /thread/post
// posts can only come on threads

// GET ALL POST wont be necessary because we can link it with the Thread id
// GET by Thread ID
// GET by User
// GET by ID
// POST post to a thread
// DELETE

module.exports = router;